// eslint-disable-next-line react/prop-types
const MensagemComponent = ({children}) => {
    return(
        <div>
            <h1>Hello {children}</h1>
        </div>
    )
}

export default MensagemComponent;